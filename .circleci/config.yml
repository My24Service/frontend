version: "2.1"

# Set `manual-build: true` when triggering a pipeline by hand to run the
# temporary `build` workflow below without the `test` job.
parameters:
  manual-build:
    type: boolean
    default: false

jobs:
  test:
    working_directory: ~/circleci-frontend
    docker:
      - image: cimg/node:22.16
    environment:
      PNPM_HOME: /home/circleci/.local/share/pnpm
      # CircleCI runs steps under bash but does not export $SHELL, which
      # pnpm's install script needs for `pnpm setup`. The profile it writes
      # is never reloaded in CI; PATH is handled via $BASH_ENV below.
      SHELL: /bin/bash
    steps:
      - checkout
      - restore_cache:
          name: Restore pnpm Package Cache
          keys:
            - pnpm-packages-{{ checksum "pnpm-lock.yaml" }}
      - run:
          name: Install pnpm package manager
          command: |
            curl -fsSL https://get.pnpm.io/install.sh | sh -
            echo 'export PATH="$PNPM_HOME/bin:$PATH"' >> "$BASH_ENV"
            "$PNPM_HOME/bin/pnpm" config set store-dir .pnpm-store
      - run:
          name: Install Dependencies
          command: pnpm install
      - save_cache:
          name: Save pnpm Package Cache
          key: pnpm-packages-{{ checksum "pnpm-lock.yaml" }}
          paths:
            - .pnpm-store
      - run:
          name: Run test suite
          command: pnpm test
      - run:
          # Contract usage gate (block A). Warns only for now: it exits 0 and
          # lists operations no client calls. Switches to a failure in block A
          # step 7 (`pnpm run usage-gate -- --strict`).
          name: Contract usage gate (warning only)
          command: pnpm run usage-gate
  build-push-main:
    working_directory: ~/circleci-frontend
    docker:
      - image: cimg/base:current-24.04
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Docker login registry.chapter3-it.io
          command: docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_TOKEN registry.chapter3-it.io
      - run:
          name: Build image
          command: docker build -t registry.chapter3-it.io/my24service/frontend-main:latest .
      - run:
          name: Push image
          command: docker push registry.chapter3-it.io/my24service/frontend-main:latest
  build-push-develop:
    working_directory: ~/circleci-frontend
    docker:
      - image: cimg/base:current-24.04
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Docker login registry.chapter3-it.io
          command: docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_TOKEN registry.chapter3-it.io
      - run:
          name: Build image
          command: docker build -t registry.chapter3-it.io/my24service/frontend-develop:latest .
      - run:
          name: Push image
          command: docker push registry.chapter3-it.io/my24service/frontend-develop:latest

workflows:
  version: 2
  # Temporary: run the image builds without `test`. Gated on the
  # `manual-build` pipeline parameter, so no automatic trigger (push, PR,
  # schedule) can start it. Trigger by hand via Trigger Pipeline with
  # `manual-build: true`.
  build:
    when: << pipeline.parameters.manual-build >>
    jobs:
      - build-push-main:
          filters:
            branches:
              only: main
      - build-push-develop:
          filters:
            branches:
              only: develop
  test-and-build:
    jobs:
      # Deliberately unfiltered. Combined with the project-level
      # "Only build pull requests" setting, this runs the suite on every PR
      # and on the default branch, and nowhere else. CircleCI branch filters
      # cannot see a PR's target branch, so this is the only way to cover
      # PRs into develop/main.
      - test
      - build-push-main:
          requires:
            - test
          filters:
            branches:
              only: main
      - build-push-develop:
          requires:
            - test
          filters:
            branches:
              only: develop
