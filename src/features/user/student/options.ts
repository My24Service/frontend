/**
 * The countries the student screens offer: the same three the legacy screens
 * hard-coded once their store lookup was commented out. A real lookup is its
 * own change.
 */
export const COUNTRY_OPTIONS = ['NL', 'BE', 'DE']
